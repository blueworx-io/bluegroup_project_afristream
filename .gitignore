node_modules/
dist/
playwright-report/
test-results/
# Page snapshots and screenshots from the Playwright MCP browser.
.playwright-mcp/

# Build-time cache for the sports TV guide grabber (npm run sync-listings).
.cache/

# The cleared IMDb challenge from `npm run sync-watchlist -- --supervised`.
# Session cookies for an account — never commit.
.imdb-session.json
